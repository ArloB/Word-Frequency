# Word-Frequency

Reads from Project Gutenberg files and produces a list of the most frequent words.

# Usage

Run the following on any file featuring the Project Gutenberg preamble. Will run until it finds the corresponding end string.

```
./tw [Nwords] File
```